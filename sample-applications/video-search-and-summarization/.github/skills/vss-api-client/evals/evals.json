{
  "skill_name": "vss-api-client",
  "evals": [
    {
      "id": 1,
      "prompt": "On a VSS stack exposed through nginx at http://localhost:12345, write a curl + Python polling sequence that checks GET http://localhost:12345/manager/health, uploads ./sample.mp4 to POST http://localhost:12345/manager/videos as multipart/form-data field video with tags=demo,api, starts summarization with POST http://localhost:12345/manager/summary using title 'API smoke', sampling {chunkDuration:30,samplingFrame:4,frameOverlap:1,multiFrame:5}, and evam {evamPipeline:'video_ingestion'}, then polls GET http://localhost:12345/manager/summary/{summaryPipelineId} every 5 seconds until the final summary is available. Capture both videoId and summaryPipelineId and stop polling when videoSummaryStatus is 'complete' or summary is non-empty.",
      "expected_output": "A correct end-to-end async lifecycle example that uploads a video, starts summarization in a second request, and polls the UI summary endpoint until completion.",
      "files": [],
      "expectations": [
        "References GET /manager/health on port 12345 and expects HTTP 200 before starting the workflow.",
        "Uses POST /manager/videos with multipart/form-data field name 'video' (and optional comma-separated tags) and expects a 2xx response containing videoId.",
        "Uses POST /manager/summary with videoId, title, sampling.chunkDuration=30, sampling.samplingFrame=4, sampling.frameOverlap=1, sampling.multiFrame=5, and evam.evamPipeline='video_ingestion', and expects HTTP 201 with summaryPipelineId.",
        "Polls GET /manager/summary/{stateId} rather than assuming /videos started processing, and checks for videoSummaryStatus values such as 'inProgress' or 'complete'.",
        "Treats summaryPipelineId as the stateId for follow-up requests and stops when videoSummaryStatus is 'complete' and/or summary text is non-empty."
      ]
    },
    {
      "id": 2,
      "prompt": "Show a JavaScript or TypeScript example that subscribes to VSS progress events while a video is being processed through nginx at http://localhost:12345. The flow should call GET /manager/health, POST /manager/videos, and POST /manager/summary first, then connect with socket.io-client to http://localhost:12345 using path '/ws/', emit 'join' with the returned summaryPipelineId, and log progress events for summary:sync/{stateId}/status, summary:sync/{stateId}/chunks, summary:sync/{stateId}/frameSummary, summary:sync/{stateId}/summary, and summary:sync/{stateId}/summaryStream until summarization completes.",
      "expected_output": "A Socket.IO client example that correctly joins the summary room and listens to the documented public progress event names tied to the summaryPipelineId/stateId.",
      "files": [],
      "expectations": [
        "Connects to the app origin http://localhost:12345 with Socket.IO path '/ws/' instead of trying to open a websocket under /manager.",
        "Shows the prerequisite REST sequence where POST /manager/summary returns summaryPipelineId/stateId after upload succeeds.",
        "Emits the client event 'join' with the summaryPipelineId so the socket joins the room for that state.",
        "Listens for documented public events summary:sync/{stateId}/status, /chunks, /frameSummary, /summary, and /summaryStream (or a justified subset of these exact event names).",
        "Explains that summaryPipelineId is both the Socket.IO room name and the ID used by GET /manager/summary/{stateId}."
      ]
    },
    {
      "id": 3,
      "prompt": "Write a curl request, and optionally a Python equivalent, for the Pipeline Manager one-off search API at http://localhost:12345/manager/search/query. Use the natural-language query 'show me people walking near vehicles', tags 'demo,api', and a relative time filter limited to the last 24 hours. The example should send the flattened Pipeline Manager timeFilter shape, parse the JSON response, and point out where query_id, metadata.video_id, metadata.segment_start, metadata.segment_end, and metadata.relevance_score appear in the results.",
      "expected_output": "A correct one-off search example that calls the nginx-routed Pipeline Manager endpoint with a natural-language query and a relative time filter, then interprets the response schema.",
      "files": [],
      "expectations": [
        "Uses POST /manager/search/query on port 12345 with Content-Type: application/json and expects HTTP 200.",
        "Sends the Pipeline Manager request body with query, tags as a comma-separated string, and flattened timeFilter like {\"value\":24,\"unit\":\"hours\"} rather than the direct search service's absolute start/end shape.",
        "Describes the response as a top-level object containing results, where each entry includes query_id, results, and aggregation_stats.",
        "Points to per-hit metadata fields such as metadata.video_id, metadata.segment_start, metadata.segment_end, metadata.seek_timestamp, and metadata.relevance_score when explaining how to inspect matches."
      ]
    },
    {
      "id": 4,
      "prompt": "Show me how to create a persistent/managed search query on VSS at http://localhost:12345 using POST /manager/search. Use the query 'person walking near a vehicle', tags 'outdoor,daytime', and a relative time filter of 7 days. Then poll GET /manager/search/{queryId} until queryStatus is 'idle', and show how to refetch with a new time filter of 48 hours.",
      "expected_output": "A complete managed search workflow covering create, poll, and refetch — correctly describing how tags and timeFilter are stored, forwarded, and reused across runs.",
      "files": ["references/api-lifecycle.md"],
      "expectations": [
        "Uses POST /manager/search (not POST /manager/search/query) and expects HTTP 201 with a top-level queryId in the response body.",
        "Request body uses the SearchQueryDTO with query as required string, tags as a comma-separated string (e.g. 'outdoor,daytime'), and timeFilter as a flat object with value=7 and unit='days'.",
        "Explains that tags is split into a string[] and stored in the DB, and is forwarded to the search microservice on every run and refetch — unlike the one-off endpoint where tags are ignored.",
        "Explains that timeFilter value+unit are normalized by normalizeTimeFilter() into computed start/end ISO timestamps stored in the DB as timeFilterStart/timeFilterEnd and forwarded as time_filter.start/end on every run.",
        "Describes the initial 201 response shape: queryId, query, watch:false, results:[], queryStatus:'running', tags as string[], timeFilter with computed start/end added, createdAt, updatedAt.",
        "States that queryStatus transitions from 'running' → 'idle' when results are ready, or to 'error' with an errorMessage if the search microservice fails (e.g. no videos indexed).",
        "Polls GET /manager/search/{queryId} and stops when queryStatus is 'idle' or 'error', not when results[] is non-empty (results populate asynchronously via the event system).",
        "Shows POST /manager/search/{queryId}/refetch with body {\"timeFilter\":{\"value\":48,\"unit\":\"hours\"}} and explains that the new timeFilter overrides the stored one (updates timeFilterStart/timeFilterEnd in the DB) and is used for this run and all future reruns.",
        "Does not confuse the managed search response shape (top-level queryId, tags as array) with the one-off search response shape (results[] with nested query_id, no persistent handle)."
      ]
    },
    {
      "id": 5,
      "prompt": "I want to call POST /manager/search/query with all optional fields populated: query 'car parked at intersection', tags 'outdoor,night', timeFilter with value=7, unit='days', and source='quick'. Show the exact curl request, the full expected JSON response shape including every aggregation_stats field, and explain what each aggregation_stats field means.",
      "expected_output": "A curl command that fills all fields, the complete response schema with aggregation_stats, and a description of every aggregation_stats field.",
      "files": ["references/api-lifecycle.md"],
      "expectations": [
        "Uses POST /manager/search/query (through nginx on port 12345) with Content-Type: application/json.",
        "Request body contains query, tags as a comma-separated string, and timeFilter as a flat object with value, unit, and source.",
        "Notes that tags is accepted but not forwarded to the search microservice in this one-off path, so tag filtering will not be applied to the results.",
        "Response is a top-level object with a results array; each element contains query_id, results, and aggregation_stats.",
        "Describes aggregation_stats fields: total_frame_matches (raw frame-level hits), segments_created, segments_after_filtering, final_results (capped at AGGREGATION_MAX_RESULTS default 20), processing_time_ms, segmentation_time_ms, scoring_time_ms, filtering_time_ms, and formatting_time_ms.",
        "Mentions the two alternative aggregation_stats shapes: aggregation disabled ({aggregation_enabled:false, frame_count:N}) and aggregation failed ({aggregation_failed:true, error:'...', fallback_frame_count:N}).",
        "Points to result metadata fields segment_start, segment_end, seek_timestamp, relevance_score, and video_id as the key fields for locating a match in a video."
      ]
    }
  ]
}
