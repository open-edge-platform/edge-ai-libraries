---
# SPDX-FileCopyrightText: (C) 2025 Intel Corporation
# SPDX-License-Identifier: Apache-2.0

name: "[GEN-AI] Multimodal-embedding-serving-CI"
run-name: "[GEN-AI-Multimodal-embedding-serving-CI] PR  workflow (by @${{ github.actor }} via ${{ github.event_name }})"


# Only run at most 1 workflow concurrently per PR, unlimited for branches
concurrency:
  group: ${{ github.workflow }}-${{ github.event_name == 'pull_request' && github.event.pull_request.number || github.sha }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

on:
  pull_request:
    branches:
      - main
    paths:
      - 'microservices/multimodal-embedding-serving/**'

jobs:
   
  trivy-scan:
    runs-on: ubuntu-22.04-32core-128GB
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          persist-credentials: false
      
      - name: Build Docker image
        run: |
          cd microservices/multimodal-embedding-serving/
          
          
          # Build multimodal embedding serving microservice (Poetry-based FastAPI)
          docker build -f ./docker/Dockerfile -t multimodal-embedding-serving:latest .
          
      - name: Verify Docker image
        run: |
          echo "=== Built Docker Image ==="
          docker images | grep multimodal-embedding-serving
          echo "=== Image Build Verification Complete ==="
      
      - name: Run Trivy Filesystem Scan
        uses: open-edge-platform/orch-ci/.github/actions/security/trivy@cda43140ad4881079bc4f8a92f1b07bddcdb2929
        id: trivy-fs
        with:
          scan_type: "fs"  
          scan-scope: "all"  
          severity: "HIGH,CRITICAL" 
          format: "json"
          scan_target: "microservices/multimodal-embedding-serving/"
          report_suffix: "-fs-multimodal-embedding-serving-CT7"
      
         
      - name: Run trivy Scan - multimodal-embedding-serving (HTML Report)
        uses: open-edge-platform/orch-ci/.github/actions/security/trivy@cda43140ad4881079bc4f8a92f1b07bddcdb2929
        id: multimodal-embedding-serving-html
        with:
          scan_type: "image"  
          scan-scope: "all"
          scan_target: "multimodal-embedding-serving:latest"  
          severity: "HIGH,CRITICAL"  
          format: "table"
          report_suffix: "-image-multimodal-embedding-serving-html-CT248"
          generate_sbom: "false"

      - name: Run trivy Scan - multimodal-embedding-serving (SPDX SBOM)
        uses: open-edge-platform/orch-ci/.github/actions/security/trivy@cda43140ad4881079bc4f8a92f1b07bddcdb2929
        id: multimodal-embedding-serving-spdx
        with:
          scan_type: "image"  
          scan-scope: "all"
          scan_target: "multimodal-embedding-serving:latest"  
          severity: "HIGH,CRITICAL"  
          format: "spdx-json"
          scanners: "vuln"
          report_suffix: "-image-multimodal-embedding-serving-spdx-CT248"
          generate_sbom: "false"

      - name: Scan Dockerfile with Trivy
        uses: open-edge-platform/orch-ci/.github/actions/security/trivy@cda43140ad4881079bc4f8a92f1b07bddcdb2929
        with:
          scan_type: "config"
          scan_target: "microservices/multimodal-embedding-serving/docker/"
          severity: "HIGH,CRITICAL"
          format: "json"
          misconfig_scanners: "dockerfile"
          report_suffix: "-config-multimodal-embedding-serving-CT222"
           
      - name: Upload Report
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: trivy-report-multimodal-embedding-serving
          path: security-results/trivy*

  bandit-scan:
    runs-on: ubuntu-22.04-32core-128GB
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
      
      - name: Run Bandit Scan
        uses: open-edge-platform/orch-ci/.github/actions/security/bandit@cda43140ad4881079bc4f8a92f1b07bddcdb2929
        id: bandit
        with:
          scan-scope: "all"  
          output-format: "txt" 
          fail-on-findings: "false"
          paths: "microservices/multimodal-embedding-serving/"
          report_suffix: "-bandit-multimodal-embedding-serving-CT161"
          
      - name: Upload Report
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: bandit-report-multimodal-embedding-serving
          path: bandit-report-*.txt
        

  clamav-scan:
    runs-on: ubuntu-22.04-32core-128GB
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
      
      - name: Run ClamAV Scan
        uses: open-edge-platform/orch-ci/.github/actions/security/clamav@27276444a9bcf247a27369406686b689933bd1ff
        id: clamav
        with:
          scan-scope: "all"  
          output-format: "txt" 
          fail-on-findings: "false"
          paths: "microservices/multimodal-embedding-serving/"
          exclude_dirs: ".git,.pytest_cache,__pycache__,.venv"
          
      - name: Upload Report
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: clamav-report-multimodal-embedding-serving
          path: security-results/clamav*

  # GitLeaks scanning job
  gitleaks-scan:
    runs-on: ubuntu-22.04-32core-128GB
    permissions:
      contents: read
    steps:
      - name: Checkout code
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          persist-credentials: false
          
      - name: Install gitleaks
        uses: open-edge-platform/orch-ci/.github/actions/bootstrap@a95228137803b756aae327668c115db235802982
        with:
          bootstrap_tools: "gitleaks"
          
      - name: Get current timestamp
        id: timestamp
        run: echo "time=$(date +%s)" >> "$GITHUB_OUTPUT"
        
      - name: Clone CI repo
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          repository: open-edge-platform/orch-ci
          path: ci
          persist-credentials: false
          
      - name: Scan for secrets
        run: |
          gitleaks dir "microservices/multimodal-embedding-serving/" -v -c ci/.gitleaks.toml --baseline-path ci/gitleaks_baselines/gitleaks-multimodal-embedding-serving.json -r gitleaks-multimodal-embedding-serving.json || true
          
      - name: Upload Gitleaks Report
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: gitleaks-report-multimodal-embedding-serving-${{ steps.timestamp.outputs.time }}
          path: gitleaks-multimodal-embedding-serving.json

  # Python linting with pylint - Multimodal Embedding Serving
  pylint-scan-multimodal-embedding-serving:
    runs-on: ubuntu-22.04-32core-128GB
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
      
      - name: Setup Python
        uses: actions/setup-python@ece7cb06caefa5fff74198d8649806c4678c61a1 # v6.3.0
        with:
          python-version: '3.12'
      
      - name: Install Poetry
        run: |
          pip install poetry
          
      - name: Install dependencies and pylint
        run: |
          cd microservices/multimodal-embedding-serving/
          poetry install || true
          poetry add --group dev pylint || true
      
      - name: Run pylint
        run: |
          cd microservices/multimodal-embedding-serving/
          mkdir -p security-results
          echo "=== Pylint Scan Results - Multimodal Embedding Serving ===" > security-results/pylint-report-multimodal-embedding-serving.txt
          echo "Scan Date: $(date)" >> security-results/pylint-report-multimodal-embedding-serving.txt
          echo "" >> security-results/pylint-report-multimodal-embedding-serving.txt
          
          # Create a basic pylint config
          cat > .pylintrc << EOF
          [MESSAGES CONTROL]
          disable=C0111,C0103,R0903,R0913,W0613,W0622,R0801,R0902,R0914,R0915,R0912,C0301,C0302,R0801
          
          [FORMAT]
          max-line-length=120
          
          [REPORTS]
          output-format=text
          reports=yes
          EOF
          
          # Find all Python files and lint them
          find src/ -type f -name "*.py" -exec poetry run pylint --rcfile=.pylintrc {} + >> security-results/pylint-report-multimodal-embedding-serving.txt 2>&1 || true
          poetry run pylint --rcfile=.pylintrc app.py >> security-results/pylint-report-multimodal-embedding-serving.txt 2>&1 || true
          
          echo "Pylint scan completed"
      
      - name: Upload pylint Report
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: pylint-report-multimodal-embedding-serving
          path: microservices/multimodal-embedding-serving/security-results/pylint-report-multimodal-embedding-serving.txt

  # ShellCheck scanning job
  shellcheck-scan:
    runs-on: ubuntu-22.04-32core-128GB
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
      
      - name: Setup environment with shellcheck
        uses: open-edge-platform/orch-ci/.github/actions/bootstrap@a95228137803b756aae327668c115db235802982
        with:
          bootstrap_tools: "shellcheck"
      
      - name: Run ShellCheck
        run: |
          mkdir -p security-results
          echo "=== ShellCheck Scan Results ===" > security-results/shellcheck-report-multimodal-embedding-serving.txt
          echo "Scan Date: $(date)" >> security-results/shellcheck-report-multimodal-embedding-serving.txt
          echo "" >> security-results/shellcheck-report-multimodal-embedding-serving.txt
          
          # Find all shell scripts and check them
          find microservices/multimodal-embedding-serving/ -type f \( -name "*.sh" -o -name "*.bash" \) -print0 | while IFS= read -r -d '' file; do
            echo "Checking: $file" >> security-results/shellcheck-report-multimodal-embedding-serving.txt
            shellcheck "$file" >> security-results/shellcheck-report-multimodal-embedding-serving.txt 2>&1 || true
            echo "---" >> security-results/shellcheck-report-multimodal-embedding-serving.txt
          done
          
          echo "ShellCheck scan completed"
      
      - name: Upload ShellCheck Report
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: shellcheck-report-multimodal-embedding-serving
          path: security-results/shellcheck-report-multimodal-embedding-serving.txt
