
debian_99_profile_reference_samples () {
  # reference samples cannot start at the same time
  [ "$1" = "start" ] || echo "pre_system_scan core_types manufacturing_ai_suites metro_ai_suites retail_ai_suites"
}

