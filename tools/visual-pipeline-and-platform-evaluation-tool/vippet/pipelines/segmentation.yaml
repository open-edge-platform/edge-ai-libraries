name: Segmentation
definition: >
  This is a segmentation pipeline preview. Segmentation is used for identifying individual objects and seperating them from the original image
tags:
  - Retail
variants:
  - name: CPU
    pipeline_description: >-
      filesrc location=/videos/input/people.mp4 !
      decodebin3 !
      videoconvert !
      video/x-raw,format=NV12 !
      gvadetect
        model=/models/output/ultralytics/public/yolo26s-seg/FP16/yolo26s-seg.xml
        device=CPU !
      queue !
      gvawatermark ! 
      gvafpscounter starting-frame=100 !
      videoconvert !
      video/x-raw,format=I420 !
      gvametapublish method=file file-path=/dev/null !
      fakesink name=default_output_sink sync=false async=false
  - name: GPU
    pipeline_description: >-
      filesrc location=/videos/input/people.mp4 !
      decodebin3 !
      gvadetect
        model=/models/output/ultralytics/public/yolo26s-seg/FP16/yolo26s-seg.xml
        device=GPU !
      queue !
      gvawatermark !
      gvafpscounter starting-frame=100 !
      gvametapublish method=file file-path=/dev/null !
      fakesink name=default_output_sink sync=false async=false
